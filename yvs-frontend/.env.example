# Frontend environment variables
# Copy to .env and adjust if your backend runs on a different port.

# The Vite dev proxy forwards /api/* to this URL automatically.
# You only need this if you change the backend port from 5000.
VITE_API_BASE=http://localhost:5000
